# Editor / agent tooling
.claude/

# OS cruft
.DS_Store
Thumbs.db

# Manual dry-run sentinel for the scheduled routines (never commit)
.testrun

# CNAME held out of deploy until Cloudflare DNS is wired (else GH Pages redirects to a dead domain)
CNAME

# Internal docs live in .claude/ (local-only). GitHub Pages serves every tracked file,
# so agent notes, setup docs and unreleased marketing never go in the published tree.
/CLAUDE.md
/README.md
/MAILCHIMP-SETUP.md
/marketing/*.md
